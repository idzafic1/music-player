{
  "name": "music-player",
  "version": "1.0.0",
  "private": true,
  "description": "Personal Music Player - Node.js/TypeScript backend + Expo frontend",
  "scripts": {
    "backend:dev": "npm --prefix backend run dev",
    "backend:build": "npm --prefix backend run build",
    "backend:start": "npm --prefix backend run start",
    "backend:migrate": "npm --prefix backend run migrate",
    "backend:test": "npm --prefix backend run test",
    "frontend:dev": "npm --prefix frontend run start",
    "frontend:web": "npm --prefix frontend run web",
    "frontend:build": "npm --prefix frontend run build",
    "test": "npm --prefix backend test"
  }
}
